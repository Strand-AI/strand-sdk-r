% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{strand_estimate}
\alias{strand_estimate}
\title{Estimate credit cost for a prediction}
\usage{
strand_estimate(client, upload_id, markers)
}
\arguments{
\item{client}{A \code{strand_client}.}

\item{upload_id}{Upload identifier (from \code{\link[=strand_upload_file]{strand_upload_file()}}).}

\item{markers}{Character vector of marker names (e.g. \code{c("CD3", "CD8")}).}
}
\value{
A list with \code{patch_count}, \code{marker_count}, \code{estimated_credits},
\code{org_balance}, \code{org_pending}.
}
\description{
Calls \code{POST /api/v1/predict/estimate} to compute the credit cost for a
given upload + marker selection. Does not reserve credits.
}
\examples{
\dontrun{
est <- strand_estimate(client, upload$id, c("CD3", "CD8", "Ki67"))
message("≈ ", est$estimated_credits, " credits")
}
}
