% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retention.R
\name{strand_set_retention_bulk}
\alias{strand_set_retention_bulk}
\title{Set retention on a batch of samples (max 500)}
\usage{
strand_set_retention_bulk(
  client,
  sample_ids,
  expires_at = NULL,
  pin = FALSE,
  use_org_default = FALSE,
  reason = NULL
)
}
\arguments{
\item{client}{A \code{strand_client} from \code{\link[=strand_client]{strand_client()}}.}

\item{sample_ids}{Character vector of sample UUIDs (length 1-500).}

\item{expires_at}{A \code{POSIXct} (or ISO 8601 character) expiry. Pass \code{NULL}
together with \code{pin = TRUE} to pin indefinitely.}

\item{pin}{If \code{TRUE}, pin the sample indefinitely (overrides org policy).}

\item{use_org_default}{If \code{TRUE}, clear any override and revert to the
org's current retention policy.}

\item{reason}{Optional governance reason (10-500 chars).}
}
\value{
A list with \code{updated} (integer count) and \code{batchId} (UUID).
}
\description{
All-or-nothing: if any sample fails the permission gate (caller is not
the sample creator, an org owner/admin, or a Strand admin), the whole
call returns a 403 with no rows touched.
}
\examples{
\dontrun{
client <- strand_client()
strand_set_retention_bulk(client, c(uuid1, uuid2, uuid3), pin = TRUE)
}
}
