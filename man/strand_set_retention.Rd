% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retention.R
\name{strand_set_retention}
\alias{strand_set_retention}
\title{Set retention on a single sample}
\usage{
strand_set_retention(
  client,
  sample_id,
  expires_at = NULL,
  pin = FALSE,
  use_org_default = FALSE,
  reason = NULL
)
}
\arguments{
\item{client}{A \code{strand_client} from \code{\link[=strand_client]{strand_client()}}.}

\item{sample_id}{UUID of the sample.}

\item{expires_at}{A \code{POSIXct} (or ISO 8601 character) expiry. Pass \code{NULL}
together with \code{pin = TRUE} to pin indefinitely.}

\item{pin}{If \code{TRUE}, pin the sample indefinitely (overrides org policy).}

\item{use_org_default}{If \code{TRUE}, clear any override and revert to the
org's current retention policy.}

\item{reason}{Optional governance reason (10-500 chars).}
}
\value{
The updated sample payload (\code{id}, \code{expiresAt}, \code{expiresAtSource}, ...).
}
\description{
Pin a sample to a specific expiry, pin indefinitely, or revert to the
org's default retention policy.
}
\examples{
\dontrun{
client <- strand_client()
strand_set_retention(client, sample_id, pin = TRUE,
                     reason = "Active research project — keep until publication")
}
}
