% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uploads.R
\name{strand_upload_file}
\alias{strand_upload_file}
\title{Upload a Whole Slide Image to the Strand Platform}
\usage{
strand_upload_file(
  client,
  path,
  content_type = NULL,
  chunk_size = 8L * 1024L * 1024L,
  progress = FALSE,
  auto_segment = NULL
)
}
\arguments{
\item{client}{A \code{strand_client} from \code{\link[=strand_client]{strand_client()}}.}

\item{path}{Path to a local file (SVS / TIFF / NDPI / ...).}

\item{content_type}{Override the auto-detected MIME type.}

\item{chunk_size}{Bytes per chunk. Must be a positive multiple of 256 KiB
(the GCS resumable upload requirement). Defaults to 8 MiB.}

\item{progress}{If \code{TRUE}, prints upload progress to the console.}

\item{auto_segment}{Opt out of automatic cell segmentation for this upload.
\code{NULL} (default) uses the org's default; \code{FALSE} skips segmentation (the
slide is still ingested and rendered); \code{TRUE} forces it on even when the
org default is off.}
}
\value{
A list with class \code{strand_upload} containing \code{id}, \code{gcs_path},
\code{upload_url}, \code{width_px}, \code{height_px}, \code{status}.
}
\description{
Performs the three-step resumable upload flow:
\enumerate{
\item \code{POST /api/v1/uploads} — create a resumable session
\item \verb{PUT <uploadUrl>} — stream the file to GCS in 8 MiB chunks
\item \code{POST /api/v1/uploads/{id}/complete} — finalize, read slide dims
}
}
\examples{
\dontrun{
client <- strand_client()
upload <- strand_upload_file(client, "slide.svs", progress = TRUE)
}
}
