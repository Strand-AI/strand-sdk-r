% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/markers.R
\name{strand_markers_list}
\alias{strand_markers_list}
\title{List available markers}
\usage{
strand_markers_list(client)
}
\arguments{
\item{client}{A \code{strand_client} from \code{\link[=strand_client]{strand_client()}}.}
}
\value{
A named list with \code{full_panel} (logical; whether the account holds
the full-panel entitlement), \code{markers} (a character vector of marker
names), and \code{public_markers} (the subset that is part of the public panel).
}
\description{
List the markers your account can request, scoped to your entitlement: a
self-signup account sees the public panel; a full-panel account sees the
whole vocabulary. Credit-free. The names returned are exactly what
\code{\link[=strand_predict]{strand_predict()}} and \code{\link[=strand_estimate]{strand_estimate()}} will accept.
}
\examples{
\dontrun{
client <- strand_client()
catalog <- strand_markers_list(client)
catalog$markers
}
}
