% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/results.R
\name{strand_download_results}
\alias{strand_download_results}
\title{Download job results and convert to a SpatialExperiment}
\usage{
strand_download_results(job, path = NULL, level = 0L, markers = NULL)
}
\arguments{
\item{job}{A \code{strand_job} from \code{\link[=strand_predict]{strand_predict()}}.}

\item{path}{Optional output directory. When set, downloads the store to
disk and returns the directory path; no \code{SpatialExperiment} is built.}

\item{level}{Pyramid level to read; \code{0} is full resolution.}

\item{markers}{Optional character vector restricting which multiscales to
load. Defaults to every multiscale except \code{"H&E"}.}
}
\value{
A \code{SpatialExperiment} with \code{assay = "expression"} and pixel
coordinates in \code{spatialCoords()}. When \code{path} is supplied, the directory
path is returned invisibly.
}
\description{
Walks the OME-Zarr result store via the API-key-authenticated proxy at
\verb{/api/v1/jobs/\{id\}/results/files/\{path\}}, assembles the per-marker arrays
into one matrix (rows = markers, columns = pixels), and returns a
\code{\link[SpatialExperiment:SpatialExperiment]{SpatialExperiment::SpatialExperiment()}} object.
}
\details{
For large slides this materializes a dense \verb{n_markers x (H * W)} matrix in
memory; pass \code{path} to mirror the zarr store to disk instead and read
selectively with \code{Rarr}.
}
\examples{
\dontrun{
job <- strand_predict(client, upload$id, c("CD3", "CD8"))
strand_job_wait(job)
spe <- strand_download_results(job)
}
}
