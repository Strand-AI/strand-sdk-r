% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retention.R
\name{strand_restore}
\alias{strand_restore}
\title{Restore an archived sample}
\usage{
strand_restore(client, sample_id)
}
\arguments{
\item{client}{A \code{strand_client} from \code{\link[=strand_client]{strand_client()}}.}

\item{sample_id}{UUID of the sample.}
}
\value{
The restored sample payload (\code{id}, \code{archivedAt}, \code{expiresAt}).
}
\description{
Sets \code{archived_at} to \code{NULL} and bumps \code{expires_at} to at least 30 days
from now so the nightly reaper does not immediately re-archive it. Caller
must have the same permissions required for \code{\link[=strand_set_retention]{strand_set_retention()}}.
}
\examples{
\dontrun{
client <- strand_client()
strand_restore(client, sample_id)
}
}
