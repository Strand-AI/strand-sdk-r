% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/public.R
\name{strand_public_samples_list}
\alias{strand_public_samples_list}
\title{List the public cohort}
\usage{
strand_public_samples_list(client, page = NULL, page_size = NULL, tag = NULL)
}
\arguments{
\item{client}{A \code{strand_client} from \code{\link[=strand_client]{strand_client()}}.}

\item{page}{1-based page number. \code{NULL} (default) reads page 1.}

\item{page_size}{Items per page (server default 48, max 100). \code{NULL} uses
the server default.}

\item{tag}{Optional public display-tag filter. An unknown tag returns an
empty page.}
}
\value{
A named list with \code{items} (a list of per-sample lists carrying
\code{public_id}, \code{title}, \code{thumbnail_url}, \code{tags}, and \code{metadata}) plus the
\code{page}, \code{page_size}, \code{total_count}, and \code{total_pages} integers.
}
\description{
List Strand's curated public cohort (paginated, newest-first). Available to
any authenticated client for free — the cohort is org-independent, so you
read it regardless of your own org, and no read spends credits.
}
\examples{
\dontrun{
client <- strand_client()
page <- strand_public_samples_list(client, tag = "tcga-coad")
vapply(page$items, function(s) s$public_id, character(1))
}
}
