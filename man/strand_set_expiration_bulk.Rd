% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expiration.R
\name{strand_set_expiration_bulk}
\alias{strand_set_expiration_bulk}
\title{Set expiration on a batch of samples (max 500)}
\usage{
strand_set_expiration_bulk(
  client,
  sample_ids,
  expires_at = NULL,
  never_expire = FALSE,
  use_org_default = FALSE,
  reason = NULL
)
}
\arguments{
\item{client}{A \code{strand_client} from \code{\link[=strand_client]{strand_client()}}.}

\item{sample_ids}{Character vector of sample UUIDs (length 1-500).}

\item{expires_at}{A \code{POSIXct} (or ISO 8601 character) expiration date.}

\item{never_expire}{If \code{TRUE}, set the sample to never expire.}

\item{use_org_default}{If \code{TRUE}, clear any custom expiration and follow
the org's current expiration policy.}

\item{reason}{Optional governance reason (10-500 chars).}
}
\value{
A list with \code{updated} (integer count) and \code{batchId} (UUID).
}
\description{
All-or-nothing: if any sample fails the permission gate (caller is not
the sample creator, an org owner/admin, or a Strand admin), the whole
call returns a 403 with no rows touched.
}
\examples{
\dontrun{
client <- strand_client()
strand_set_expiration_bulk(client, c(uuid1, uuid2, uuid3), never_expire = TRUE)
}
}
