% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jobs.R
\name{strand_job_wait}
\alias{strand_job_wait}
\title{Block until a job reaches a terminal status}
\usage{
strand_job_wait(job, timeout = Inf, poll_interval = 2, progress = FALSE)
}
\arguments{
\item{job}{A \code{strand_job} from \code{\link[=strand_predict]{strand_predict()}}.}

\item{timeout}{Max seconds to wait. \code{Inf} to wait forever.}

\item{poll_interval}{Seconds between status polls.}

\item{progress}{If \code{TRUE}, prints a one-line status update per poll.}
}
\value{
The terminal job status list. \code{status = "completed"} means every
requested marker delivered; \code{"partial_failed"} means some markers
delivered (results are downloadable) and the rest terminally failed after
server-side retries — it returns normally rather than erroring, so check
\verb{$status}. Only a \code{"failed"} terminal status (nothing delivered) raises a
\code{strand_job_failed_error}.
}
\description{
Polls \code{GET /api/v1/jobs/{id}} on a fixed interval and returns the terminal
status. (The platform also exposes an SSE stream at \verb{/jobs/\{id\}/stream}; the
R SDK uses polling for compatibility — there is no widely-used SSE client in
base R, and httr2 streaming is awkward to integrate with \verb{R CMD check}.)
}
