% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uploads.R
\name{strand_uploads_list}
\alias{strand_uploads_list}
\title{List uploads for the calling organization}
\usage{
strand_uploads_list(client, limit = 100L, cursor = NULL)
}
\arguments{
\item{client}{A \code{strand_client}.}

\item{limit}{Page size (1-200). Defaults to 100.}

\item{cursor}{Opaque cursor from a prior response's \code{next_cursor}. \code{NULL}
(default) fetches the first page.}
}
\value{
A list with class \code{strand_upload_list} containing:
\itemize{
\item \code{uploads}: list of \code{strand_upload} entries (each carries \code{id},
\code{filename}, \code{file_size}, \code{status}, \code{gcs_path}, \code{width_px}, \code{height_px},
\code{created_at}).
\item \code{next_cursor}: cursor for the next page, or \code{NULL} on the last page.
}
}
\description{
Fetches one page of uploads, newest-first. Filters out archived uploads;
use \code{\link[=strand_restore]{strand_restore()}} to bring archived rows back into the active set.
}
\examples{
\dontrun{
page <- strand_uploads_list(client, limit = 20)
for (u in page$uploads) message(u$id, " ", u$filename)
}
}
