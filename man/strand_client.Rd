% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{strand_client}
\alias{strand_client}
\title{Create a Strand Platform client}
\usage{
strand_client(api_key = NULL, base_url = NULL, timeout = 60, user_agent = NULL)
}
\arguments{
\item{api_key}{Bearer API key (\code{sk-strand-...}). Defaults to the
\code{STRAND_API_KEY} environment variable.}

\item{base_url}{Platform base URL (without \verb{/api/v1}). Defaults to
\code{STRAND_BASE_URL} env var, then \verb{https://app.strandai.com}.}

\item{timeout}{Per-request timeout in seconds.}

\item{user_agent}{Override the default User-Agent string.}
}
\value{
An object of class \code{strand_client}: a list with \code{api_key},
\code{base_url}, \code{api_root}, \code{timeout}, and \code{user_agent}.
}
\description{
Constructs a client object that bundles authentication, base URL, and
a shared \code{httr2} request template. The returned list is passed to every
other function in this package.
}
\details{
Default base URL is \verb{https://app.strandai.com}. The \verb{/api/v1/} path is
appended automatically; pass the root site URL, not the API root.
}
\examples{
\dontrun{
client <- strand_client()  # reads STRAND_API_KEY
client <- strand_client(api_key = "sk-strand-...",
                         base_url = "http://localhost:3000")
}
}
